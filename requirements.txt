requests==2.34.2
tiktoken==0.13.0
plyer==2.1.0
fastapi==0.111.0
uvicorn==0.30.1
# uvicorn 0.30.1 依赖 websockets 的 legacy 协议实现；websockets>=14 移除了 legacy，
# 会导致 WS 握手/帧处理损坏（浏览器 open 后秒断、python 客户端 404）。钉在 12.0。
websockets==12.0
numpy>=1.24.0
