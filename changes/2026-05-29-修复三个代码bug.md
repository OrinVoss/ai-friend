# 修复三个代码 bug

**时间**：2026-05-29

## 修改文件

- `core/agent.py` — 修复连续负面计数初始化覆盖 + 删除重复梦境方法

## Bug 1：_consecutive_negative 双重初始化

第 88-89 行根据情绪设置初始值（4/2），但第 93 行无条件覆盖为 0。导致愤怒的 AI 开局不接近破防线。

修复：`self._compressing` 提到前面单独初始化，`else` 分支正确设 `_consecutive_negative = 0`，删除第 93 行的覆盖。

## Bug 2：process_dream 和 _generate_dream 功能重复

`process_dream`（旧版，213 行）从未被调用，`_generate_dream`（新版，更简洁）是实际使用的版本。

修复：删除 `process_dream`，保留 `_generate_dream`。

## Bug 3：process_message 和 _on_think 重复逻辑

两个方法各自构建 system prompt + 组装 messages，约 30 行重复代码。

状态：已在 #14 / #58 中跟踪，暂未修复。
