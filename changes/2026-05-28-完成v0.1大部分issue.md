# 完成 v0.1 大部分 issue

**时间**：2026-05-28

## 修改文件

- `config.py` — 新增 `api_timeout`、环境变量覆盖 (DEEPSEEK_API_KEY 等)
- `config.json` — 补全缺失字段 (api_timeout/max_facts 等)
- `core/provider.py` — timeout 配置化，不再硬编码 180s
- `core/agent.py` — 修复上下文压缩在 process_message 路径不触发的问题
- `core/agent.py` — 清理 bare except，替换为具体异常类型
- `memory/short_term.py` — ConversationBuffer 添加 threading.Lock，format_for_prompt 改为保留最新对话
- `storage/database.py` — Schema 迁移改为 PRAGMA table_info 检查列存在性
- `storage/database.py` — reflections 表新增 is_active 列，修复 bare except
- `storage/repository.py` — prune_reflections 改为软删除 (UPDATE is_active=0)
- `tools/file_tools.py` — ReadFileTool 限制路径在项目目录内
- `web/session.py` — 传递 timeout 到 KimiProvider

## 关闭的 issue (14个)

| # | 标题 |
|---|------|
| #3 | 配置管理：支持环境变量覆盖 (被 #13 覆盖) |
| #11 | Session 泄漏 (被 #59 覆盖) |
| #12 | 竞争条件：多 proactive_loop 冲突 (被 #59 覆盖) |
| #13 | API Key 改为环境变量读取 |
| #14 | 工厂函数 (被 #58 覆盖) |
| #15 | 情感分析重复调用 (被破防重构修复) |
| #16 | ReadFileTool 路径穿越 |
| #17 | ConversationBuffer 加锁 |
| #18 | bare except 清理 |
| #31 | 统一 CLI/API 双代码路径 (被 #58 覆盖) |
| #33 | 上下文压缩永不触发 |
| #34 | config.json 与 config.py 默认值不一致 |
| #35 | Schema 迁移每次启动都执行 |
| #36 | Reflections 软删除 |
| #37 | format_for_prompt 截断方向错误 |
| #38 | timeout 配置化 |

## 保留开放的 issue (5个)

#1 异步数据库驱动、#2 结构化输出、#10 会话隔离、#30 拆分 Agent、#32 DeepSeek tokenizer——属于大型重构，留待后续迭代。
