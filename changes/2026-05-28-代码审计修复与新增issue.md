# 代码审计：修复严重 bug + 新增 10 个 issue

**时间**：2026-05-28

## 修改文件

- `core/agent.py` — 添加 `import json`，修复 token 估算的 `str(list)` 问题

## 修复内容

### #9 缺失 import json
`_react_loop` 中的 `except (json.JSONDecodeError, ValueError, KeyError)` 使用了 `json` 模块但文件顶部未导入，导致运行时 NameError。

### #3 token 估算严重不准确
```python
# ❌ 旧：str([dict, dict, ...]) 包含 Python repr 开销
estimate_tokens(str(messages[-5:]))

# ✅ 新：仅提取实际内容
" ".join(m["content"][:200] for m in messages[-5:] if m["role"] != "system")
```

`str(messages)` 同样替换为 `sum(estimate_tokens(...))`。

## 新建 issue（#68-#75）

| # | 严重度 | 问题 |
|---|--------|------|
| #68 | 🔴 严重 | process_message 绕过状态机，current_input/current_memory_context 未设置 |
| #69 | 🔴 严重 | 破防机制 Web 路径情感分析滞后一轮 |
| #70 | 🟡 中等 | 工具调用循环后续轮 token 限制过低 (128) |
| #71 | 🟡 中等 | _compress_context 缺少递归保护 |
| #72 | 🟢 性能 | reversed(get_all()) 频繁创建迭代器 |
| #73 | 🟡 中等 | personality.save 在 _react_loop 和 _on_reflect 中重复保存 |
| #74 | 🟡 中等 | _tool_registry 初始化为 None 可能 NPE |
| #75 | 🔴 严重 | CLI 路径 sentiment + consecutive_negative 重复计算两次 |
