# 2026-07-26 新增修复计划 doc/fix-plan-2026-07-26-四问题修复.md

## 修改原因

用户提出四项潜在架构问题（run_async 死锁、CognitiveState 一致性、inner_drive_state
淘汰策略、工具结果归因），要求整理成修复计划交给其他模型执行。

## 修改的文件

- `doc/fix-plan-2026-07-26-四问题修复.md`（新增）

## 核实结论（写计划前已逐条对照代码）

1. **run_async**：原描述机制不准确——主事件循环不会被阻塞（web/server.py:189,478 走
   默认执行器），真实风险是 4 线程池 `_EXECUTOR` 的嵌套调用饿死（60s 级卡顿）；
   方案为 thread-local 重入防护 + 调用路径审计测试。
2. **CognitiveState**：属实，三处具体问题——决策后改写 state（message_handler.py:313-314）、
   轻量渲染逻辑两处重复（inner_drive.py:304-309 与 message_handler.py:607-612）、
   _run_agent3 冗余再检索（message_handler.py:616-623）。
3. **inner_drive_state 淘汰**：原描述已过时——`_evict_if_needed`（inner_drive_state.py:292-304）
   当前已是 priority 淘汰（二期，非 FIFO），无需改代码，只补回归测试。
4. **工具结果归因**：属实——ToolCallRecord 无 request 字段，多请求合并后无法归因；
   方案为增加 request 字段 + format_for_phase2 分组渲染。
