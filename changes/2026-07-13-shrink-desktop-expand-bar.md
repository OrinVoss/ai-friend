# 缩小桌面端面板展开条

## 问题

桌面端面板收起后的展开条是 100% 高度、28px 宽的一大长条，视觉上过于突兀。

## 改动

- `web/static/style.css`
  - `.panel-expand` 从满高改为 48px 高、22px 宽的小块。
  - 位置从贴顶改为距顶部 12px。
  - SVG 箭头从 18px 缩小到 16px。
  - 圆角从 12px 缩小到 10px，更小巧。

## 验证

- 桌面端面板收起后，边缘只显示一小块展开按钮。
- 点击后仍可正常展开面板。
