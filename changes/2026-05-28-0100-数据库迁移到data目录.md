# 数据库迁移到 data 目录

**时间**：2026-05-28 01:00

## 修改文件

- `config.py` — `db_path` 默认值从 `ai_friend.db` 改为 `data/ai_friend.db`
- `storage/database.py` — 初始化时自动创建目录 `os.makedirs()`
- `.gitignore` — 旧规则替换为 `data/` 目录忽略

## 修改原因

用户要求数据库文件放在文件夹内，不在项目根目录。

## 清理

- 删除了根目录下的 `ai_friend.db`、`ai_friend.db-shm`、`ai_friend.db-wal`
