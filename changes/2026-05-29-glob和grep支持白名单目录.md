# glob 和 grep 支持白名单目录

**时间**：2026-05-29

## 修改文件

- `tools/search_tools.py` — glob/grep 改用 `_resolve_search_path`，支持所有白名单目录

## 问题

glob 和 grep 的 path 参数被限制在项目根目录内，无法搜索 D:\音乐、D:\桌面 等白名单路径。

## 修复

`_resolve_search_path()` 检查 path 是否在 ALLOWED_READ_ROOTS 中任一目录下，允许跨白名单搜索。
