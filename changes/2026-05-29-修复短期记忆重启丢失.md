# 修复短期记忆重启丢失

**时间**：2026-05-29

## 修改文件

- `storage/repository.py` — 新增 `get_recent_turns()` 方法
- `web/session.py` — WebAgent 初始化时从 DB 恢复最近 30 轮对话

## 修改原因

Fix #98：刷新浏览器/重启服务器后，`ConversationBuffer` 被清空，AI 不记得刚聊了什么。

## 修改内容

WebAgent 创建时，从 `conversation_turns` 表读取最近 30 轮对话，恢复到 `short_term`。
