# 2026-07-26 全面核对更新 doc 文档与 README

## 修改原因

doc/ 下文档最后同步于 2026-07-22 前后，之后的代码变更（MessageHandler 拆分、core 模块合并、
agent2_error 元数据化、dispatcher_output_cap/stream_max_bytes、2026-07-26 前端双主题升级、
embedding 端口 H-04 派生等）未反映。按 CLAUDE.md「README 和 doc/architecture.md、doc/api.md、
CLAUDE.md 保持同步更新」的要求，逐份对照当前代码实事求是核对更新。

## 修改的文件

- `doc/api.md`：补 `/api/tools/metrics` 端点；`_proactive_loop` → `RuntimeDriver`；WS 断开改为
  M-12 延迟销毁语义；segment 前端行为改为增量流式渲染；`_split_segments`/`_calc_delay` 标注为
  已删除的历史实现；补 A1 token 鉴权（401 / WS close 4001）；SQLite 表说明对齐 schema v6。
- `doc/architecture.md`：人格 JSON 无 `id` 字段；删 Agent 2 "temp=0.3"（实际沿用默认 0.8）；
  工具 9 → 10（新增 history_search，仅 Agent 3）；core 模块 23 → 25（补 agent_wiring.py /
  message_builder.py）；proactivity/cognitive_state 并入说明；Web 输出现状（分段禁用、流式渲染）。
- `doc/config-reference.md`：补 `emotion_turn_seconds`；embedding_endpoint 端口派生（H-04）；
  playfulness/thoughtfulness 特质效果更正为代码实际行为。
- `doc/deployment.md`：嵌入服务探测/启动端口从 endpoint 派生；bat 固定 8080 的注意事项。
- `doc/development-log.md`：统计更新（394 提交 / 841 测试 / 第 59 天）；新增第十卷
  「重构收尾与前端双主题（7月22日—26日）」补记 8 个遗漏提交。
- `doc/known-issues.md`：条目 5 Agent 2 超时控制标注已修复（L4-2）；#233 行号修正。
- `doc/message-flow.md`：CLI 主动触发改由 RuntimeDriver 承担；MemoryPorter/CareList 更正为
  InnerDriveState；Agent 3 工具补 history_search；PERCEIVE 读表改 facts_v2/insights_v2；
  统一固化格式更正为 SUMMARY:/TONE:/SIGNIFICANCE: 分块；分段函数已删除。
- `doc/personality-guide.md`：humor/sass 补 >0.5 触发阈值；空 conversation_examples 仍注入
  风格 stub；first_run_greeting 默认文案空格修正。
- `doc/prompt-reference.md`：删除不存在的 "Block 6c CognitiveState"；Block 7 实际渲染全量
  registry；Block 8 最近 3 条；CARE_CLUE_PROMPT / CONTRADICTION_VERIFY_PROMPT 变量名更正。
- `doc/startup-flow.md`：启动步骤顺序更正（setup_logging → web.server 导入触发 load_config →
  重设 log_level）；中间件链补 _token_auth；schema v2 → v6；WS init 启动 RuntimeDriver。
- `doc/systematic-solution.md`：Layer 0 例外均已修复（UK-001、session 校验、embedding 读回）；
  dispatcher 截断 2000 字符（dispatcher_output_cap）；测试数 841；"下一步"4 条过时事项标注。
- `doc/technical.md`：core 25 模块、851 用例、8 业务表；删 Agent 2 temp=0.3；熔断 60s；
  上下文压缩参数（6 条/120 字/12000 预算）；error_type 新枚举；分段推送图更新为现状；
  配置示例补 dispatcher_output_cap / stream_max_bytes。
- `doc/testing-guide.md`：838 → 841；删 test_segmentation.py；方法名 promote_fact/contradict_fact。
- `doc/tool-development.md`：Tool 基类补 timeout_seconds/is_internal；check_permission 已在
  dispatcher 强制调用；注册方式统一为 session_factory.assemble_session；error_type 枚举更正；
  超时标 network_error；Agent 3 工具补 history_search。
- `README.md`：Agent 2 温度表述、工具数 10 + history_search 行、双主题、theme.css 变量规范、
  RuntimeDriver 流程图、MemoryLifecycleManager 方法名。

## 备注（核对中发现的疑似代码问题，未改代码）

- `core/message_handler.py:672-675`：H-01 注释声称 Agent 3 prompt 与内部工具 registry 一致，
  但 `build_system_prompt(tools=a.tool_registry)` 实际渲染全量工具（含外部工具），仅执行侧
  收窄为内部。文档已按代码现状记录，疑似代码 bug，建议后续确认。
