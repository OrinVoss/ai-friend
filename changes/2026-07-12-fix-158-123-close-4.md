# 修复 #158 #123 + 关闭 4 个已修里程碑 issue

## 修复

### #158 — WebSocket 连接限制 + Origin 校验修复
- `web/server.py` — 添加 `_ws_connections` 全局连接跟踪列表；`websocket_endpoint` 添加每 IP 最多 5 连接、全局最多 100 连接的限制；`origin` 校验改用 `urlparse` 精确匹配 hostname 而非 `startswith` 绕过风险；`finally` 断开连接时清理跟踪列表

### #123 — REST session 定期清理
- `web/session.py` — `SessionManager` 添加 `_create_count` 计数器；`get_or_create` 每新建 10 个 session 触发一次 `cleanup_old()`

## 关闭
- #296 FactChecker 审查报告（非代码 bug）
- #109 分段发送（已有 markdown 流式 + 情绪驱动延时）
- #151 Web 绕过状态机（设计使然）
- #57 Web 持久化（已全面修复：shutdown 保存、WAL checkpoint、debounce）
- #158 WebSocket 安全（已修复）
- #123 REST session 泄漏（已修复）
