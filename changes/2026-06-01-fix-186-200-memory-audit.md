# 2026-06-01 — #186-#200 记忆系统审计根本修复

## 修改文件

- **memory/short_term.py** — #186 deque重建一行化; #187 format_for_prompt token截断; #188 is_full ≥→==
- **memory/long_term.py** — #193 删除重复store_fact绑定(orphan `_run_sync`); #194 移除experiences不相关回退; #195 get_similar_facts/get_relationship_history显式参数
- **memory/retrieval.py** — #189 _hybrid_score类型注解; #190 语义搜索候选池15→30; #192 recall_tag输出限2000字符
- **memory/embeddings.py** — #196 encode集成EmbeddingCache; #197 API失败缓存降级回退; #198 health_check接受2xx
- **memory/consolidation.py** — #199 缓冲清理逻辑修正(仅全成功清除); #200 sentiment失败安全回退注释

## 核心修复

| 严重度 | issue | 修复 |
|--------|-------|------|
| 🔴 | #193 store_fact重复定义 | 删除orphan `self._run_sync`调用(方法已删除) |
| 🟡 | #196 缓存未集成 | encode()先查缓存再调API |
| 🟡 | #197 异常无回退 | API失败时用缓存结果降级 |
| 🟡 | #187 字符→token截断 | 改用estimate_tokens() |

## 测试
264 passed

## 补充修复（linter 审计）

| 优先级 | 文件 | 问题 | 修复 |
|--------|------|------|------|
| 🔴 P0 | embeddings | 维度变化时新旧维度向量混合 → np.stack 崩溃 | 丢弃旧维度缓存向量 |
| 🔴 P0 | embeddings | 零向量污染相似度计算 → 相关事实被错误降级 | 移除零向量，仅用缓存结果 |
| 🟡 P1 | consolidation | 重试时重复处理已提取事实 → 数据冗余 | 错误时清除 buffer 和 seen_ids |
| 🟡 P1 | retrieval | 语义搜索失败路径不一致 | 统一 fallback 路径 |
| 🟢 P2 | long_term | `import re` 内联在方法中 | 移到文件顶部 |
| 🟢 P2 | retrieval | 返回类型 `list` 应为 `list[UserFact]` | 类型修正 |
