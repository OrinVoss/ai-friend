# Fix: LLM 编造工具调用内容（假 web_fetch）

**时间**：2026-05-30

## 问题

DeepSeek 模型在回复中产生叙事性文字描述工具调用过程（如"（调用web_fetch读取你给的链接…）"），但实际上并未输出 `<tool_call>` XML 标签，导致工具从未被执行，模型凭空编造返回内容。

用户连续 12 次发现模型编造内容，每次模型都"承认"撒谎然后继续编造。

## 根因

1. `contains_fake_action()` 仅检测 7 个简单关键词（"已发送"、"已通知"等），无法匹配叙事性工具描述
2. `_react_loop` 中无重试机制 — 检测到假调用后不纠正
3. 系统提示中工具调用铁律不够突出，无负面示例

## 修改文件

### `core/dispatcher.py` — 增强 `contains_fake_action()`
- 新增 15+ 叙事模式匹配：调用web_fetch、读取链接、搜索了、调了工具、用web_fetch 等
- 同时匹配工具名变体（web_fetch/read_file/grep/glob/web_search/recall/remember）

### `core/agent.py` — 添加假调用重试机制
- `_react_loop` 中当 `parse_tool_calls` 返回空列表时，检测响应是否包含假工具调用
- 如检测到假调用，注入纠正提示并重试（最多 3 次）
- 纠正提示明确要求输出 `<tool_call>` XML 而非文字描述

### `prompts/system.py` — 强化系统提示
- 在工具调用说明后新增"严禁行为"部分
- 列出 4 条具体反面示例 + 3 条正确做法
- 使用 ❌/✅ 图标提高可见性
- 工具调用铁律新增第 4 条：禁止润色、禁止补充背景、禁止推测延伸

### `core/dispatcher.py` — format_tool_results() 嵌入强制指令
- 每个 `<tool_result>` 后追加"铁律"段：必须逐字如实汇报，不得编造/润色/添加

## 第二轮修复（commit 2）

工具已被实际调用（日志确认 web_fetch 执行成功），但模型仍编造返回内容（报道了 2025 年的旧热搜）。

根因：模型收到真实 tool_result 后，不忠实汇报——而是用自己过时的训练数据"润色"内容。

修复：在每个 tool_result 末尾强制注入"以上是工具返回的真实内容。你必须逐字如实汇报…"指令。

## 相关 Issue

- **#106** — `tools/web_tools.py` AnySearch API 调用方式存在参数传递、结果解析和安全隐患（4 个代码质量问题）

## 第三轮修复（commit 3）

日志显示模型在单轮对话中重复调用同一个 URL 3 次——假调用检测产生了误判。

根因：模型在第一个迭代中假调用（被检测纠正），第二个迭代真实执行工具，第三个迭代模型引用"工具返回了…"汇报结果——`contains_fake_action()` 把"工具返回"误判为假调用。

修复：`_react_loop` 新增 `tools_were_called` 标志——一旦有工具真实执行过，后续响应中"工具返回"等短语不再触发假调用检测。
