# 存储层架构深度审查报告

**时间**：2026-05-31
**审查文件**：`storage/database.py`、`storage/repository.py` 及上下游依赖
**输出报告**：`doc/round01-storage-layer.md`

## 修改记录

- 新增 `doc/round01-storage-layer.md` — 存储层架构深度审查报告（约 9000 字）

## 审查摘要

本次审查覆盖数据库连接管理、SQL 安全性、Repository 模式、事务管理、Schema 定义与迁移、并发安全性、架构与性能问题、数据完整性风险等八个维度。

**关键发现**：
- Critical 级问题 2 个（`consolidation.py` async/await 缺失导致崩溃；`_run_sync` 跨线程死锁风险）
- High 级问题 8 个（单连接瓶颈、无 session_id 隔离、缺 aiosqlite 依赖等）
- Medium 级问题 14 个（事务粒度粗、Schema 版本化缺失、JSON 存储无校验等）
- Low 级问题 6 个

**P0 修复建议**：
1. 修复 `memory/consolidation.py:_embed_new_items()` 的 async/await 缺失
2. 添加 `aiosqlite` 到 `requirements.txt`
3. 为 `conversation_turns` 添加 `session_id` 列
4. 修复 `_run_sync` 线程安全问题
