# 2026-06-01 - 根本解决 #127-#142：业务逻辑补全

## 修改文件

### 新建
- **core/async_utils.py** — 统一 `run_async()` 桥接，替换 `long_term.py` 和 `repository.py` 中各自重复的 `_run_sync`

### 修改
- **prompts/templates.py** — FACT_EXTRACTION_PROMPT 增加主体识别规则：只提取用户自身信息，排除 AI 行为/AI 承诺/系统属性/无法验证的推测；增加 fact_type 输出字段
- **memory/consolidation.py** — 解析 fact_type 字段，只存储 `user_fact`，跳过 agent_fact/system_fact
- **storage/repository.py** — 移除重复 `_run_sync`，使用 `core/async_utils.run_async`；search_facts 和 get_active_facts SQL 加 `fact_type='user_fact'` 过滤；新增 `get_relationship_history()` 查询时间序列
- **memory/long_term.py** — 移除重复 `_run_sync`，使用 `run_async`；新增 `get_relationship_history()` 同步包装
- **core/message_handler.py** — `_build_messages` 跳过舞台指示（括号开头的对话）和 is_tool_claim 标记的轮次

## 解决的问题

| # | 修复内容 |
|---|---------|
| #127 | LLM 只提取 user_fact；检索 SQL 过滤非 user_fact |
| #130 | 对话构建时跳过舞台指示（"（调用..."等） |
| #132 | get_relationship_history() 可读时间序列 |
| #134 | 统一 run_async，删除三处重复 _run_sync |

## 测试
250 passed, 8 skipped
