# 补齐 2026-07-12 新增功能的日志

## 背景

昨天代码审计和修复批次中新增了大量功能（rate limit、CORS/CSP、message handler 输入清洗、CLI Agent2 多轮工具、embedding server 生命周期、FactChecker 改进、话题去重等）。部分关键路径缺少日志，导致线上排查时难以确认是否命中。本次只补日志，不改动业务逻辑。

## 改动

- `web/rate_limit.py`
  - `RateLimiter.is_allowed`：增加允许/拒绝/过期清理的 debug/warning 日志
  - `RateLimitMiddleware.dispatch`：记录被拦截的 API 请求

- `web/server.py`
  - `ensure_session`： lifespan 未执行时同步兜底增加 warning
  - WebSocket 消息限流分支增加 warning

- `core/cli_controller.py`
  - Agent 2 多轮工具循环：每轮开始、multi-request、in-round retry、review、re-decide、最终结果合并均增加 info 日志

- `core/message_handler.py`
  - `_sanitize_input`：检测到注入模式或截断输入时增加 warning

- `web/session.py`
  - `_save_personality_debounced`：记录防抖跳过和实际保存

- `memory/fact_checker.py`
  - embedding / keyword 矛盾检测未命中时增加 debug 日志（best_sim / best_overlap）

- `tools/search_tools.py`
  - 搜索缓存驱逐最旧条目时增加 debug 日志

- `tools/traits.py`
  - 新增 `logging` 导入；`ToolRegistry.check_permission` 拒绝时增加 warning

- `core/embedding_server.py`
  - `kill_existing_llama` 失败时增加 warning（不再静默吞异常）

- `core/proactivity.py`
  - `pick_proactive_topic` 记录最终选择、被过滤数量、最近话题列表

- `models/personality.py`
  - `record_emotion_event` 中事件被驱逐时增加 debug 日志

## 验证

- `python -m py_compile` 对以上所有文件通过
