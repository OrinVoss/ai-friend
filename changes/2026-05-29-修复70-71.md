# 修复 #70 #71：工具调用 token + 压缩递归保护

**时间**：2026-05-29

## 修改文件

- `core/agent.py` — 工具调用后续轮 max_tokens + _compress_context 递归保护

## 修复 issue

### #70 工具调用后续轮 token 限制

旧：后续轮固定 `max_tokens=128`，工具结果较长时不够用
新：`max(256, max_tok // 2)`，确保有足够空间解释工具结果

### #71 _compress_context 递归保护

新增 `_compressing` 标志位，压缩期间设为 True，防止 `provider.generate()` 内部触发压缩导致重入。
