# KimiProvider → DeepSeekProvider 重命名

将 `KimiProvider` 更名为 `DeepSeekProvider`，反映实际使用的 API 服务商。

## 修改范围
- `core/provider.py` — 类名定义
- `main.py`、`web/session.py` — 导入和实例化
- `tests/` 下 6 个测试文件
- `doc/` 下 6 个文档文件
- `changes/` 下历史记录文件
- `README.md`、`CLAUDE.md`

共 26 个文件，全部通过编译和测试。
