# 睡眠窗口调整 + 移除人工 guard

**时间**：2026-05-30

## 修改文件

- `core/agent.py` — 夜间睡眠窗口从 `23:30-0:30` 调整为 `23:00-01:00`
- `core/agent.py` — 移除 `_last_sleep_msg_time` 5 分钟 guard，靠 `.sleep_state` 文件持久化保证稳定性
- `core/agent.py` — `_last_sleep_msg_time` 初始值从 `0` 改为 `time.time()`（后因移除 guard 一并删除）

## 修改原因

1. 延长夜间睡眠窗口覆盖更合理的入睡时间
2. 取消人工 guard，靠文件持久化 + 自然睡眠/唤醒逻辑运转，重启后不会重复触发
