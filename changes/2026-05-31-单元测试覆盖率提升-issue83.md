# Issue #83：单元测试覆盖率提升

**时间**：2026-05-31

## 概述

新增 106 个单元测试，覆盖 5 个之前完全没有测试的高复杂度模块。
测试总数：65 → 171

## 新增测试文件

| 文件 | 用例数 | 覆盖模块 | 测试内容 |
|------|--------|---------|---------|
| `tests/test_emotional_state.py` | 38 | `models/personality.py` | shift（惯性/钳制/怨恨累积）、decay（独立衰减率/怨恨干扰）、_cross_modulate（压制/反制/上限）、dominant_emotion（偏向）、序列化（往返/缺失字段）、情绪事件、mood shift |
| `tests/test_provider.py` | 10 | `core/provider.py` | 连接重试（2次成功）、HTTP 5xx 重试/4xx 不重试、全部耗尽、流式解析、空流、thinking 参数、畸形 JSON 跳过 |
| `tests/test_dispatcher.py` | 21 | `core/dispatcher.py` | parse_tool_calls（单个/多个/think 剥离/裸 JSON/无效/空/nested）、execute（成功/未知工具/多个）、format_tool_results（成功/失败/铁律）、contains_fake_action（叙事/完成声明）、_normalize_args（别名映射） |
| `tests/test_personality_core.py` | 12 | `core/personality.py` | estimate_emotional_impact（边界 0.3/-0.3、分享/能量/特质阈值）、apply_emotional_shift、load（不存在/损坏 JSON）、save/load 往返 |
| `tests/test_segmentation.py` | 18 | `web/server.py` | _split_segments 全部 6 级 fallback、空/单字符/纯标点/合并、_calc_delay 速度排序/长度影响 |

## 修改文件

- `tests/test_dispatcher.py`（新建）
- `tests/test_emotional_state.py`（新建）
- `tests/test_personality_core.py`（新建）
- `tests/test_provider.py`（新建）
- `tests/test_segmentation.py`（新建）
