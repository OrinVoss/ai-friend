# 审计修复·阶段 0-5 汇总

日期：2026-07-17

来源：`doc/refactor/audit-report-2026-07-17.md` + `doc/refactor/cross-module-issues.md`（2026-07-17 全代码库 43 路并行 agent 审计：37 个跨模块问题 + 40 个"未修复"Issue 复核）。

阶段 3 的 #217/#161/#251 详见 `changes/2026-07-17-记忆层三修复-issue217-161-251.md`；阶段 4 全部 12 项详见 `changes/2026-07-17-审计修复-阶段4-tools-prompts-config.md`。本文按阶段索引全部修复，每项一行，不重复展开。

## 阶段 0：并发与锁

- #291：`Personality` 可变状态（情绪/破防等）读写加 `threading.RLock`——Issue 早已关闭但 fix 从未提交（`core/personality.py`、`core/agent.py`、`core/sleep_manager.py`）。
- H-05：proactive/explore 路径调 `_react_loop` 传 `skip_post_process=True`，不再拿上一轮用户消息重复做情绪分析（`core/message_handler.py`）。
- H-03：`Database` 跨事件循环失效的 `asyncio.Lock` 改为 `threading.Lock`，`SleepManager` 文件读写同步加锁（`storage/database.py`、`core/sleep_manager.py`）。

## 阶段 1：数据一致性与 Agent 3

- H-06：`Personality.save()` 改为先读磁盘合并再写回；`PersonalityConfig.from_dict` 拷贝保留未知字段，不再覆盖用户在线编辑（`core/personality.py`、`models/personality.py`）。
- H-08/#285/H-02：`bulk_update_embeddings` 加 session 过滤并补 commit；`_embed_new_items` 扩展到 facts_v2/observations，读写两路均按 session 过滤（`storage/repository.py`、`memory/consolidation.py`）。
- M-06：`rule_tools` 支持传入隔离注册表参数，`INNER_DRIVE_CHECKLIST` 去硬编码外部工具名（`prompts/`、`core/inner_drive.py`、`core/message_handler.py`）。
- H-01/H-07：Agent 3 三处调用统一 internal registry 并复用缓存；`_tool_failures` 改为每消息重置，不再跨消息累积（`core/message_handler.py`、`core/agent.py`）。
- M-04：`review()`/`re_decide()`/`assess_proactive()` 统一走 `_context_summary_for()`，不再绕过 `use_memory_agent` 开关（`core/inner_drive.py`、`prompts/system.py`）。

## 阶段 2：Web 会话与主动性

- M-12/#210：WS 断开改 `schedule_remove` 60 秒宽限 + 重连归属判断，不再立即销毁 session（`web/server.py`、`web/session.py`）。
- M-16：REST `/api/chat` 改 `run_in_executor`，不再阻塞事件循环（`web/server.py`）。
- L-04：`ensure_session()` 改 async、`open` 幂等，不再在已有事件循环中 `asyncio.run()`（`web/server.py`）。
- #146：`assess()` 加兜底降级——MemoryAgent 异常/为空时回退经典检索，数据流不再断裂（`core/inner_drive.py`）。
- #276：personality 防抖保存的 check-then-set 加 `threading.Lock`（`web/session.py`）。
- M-11/#177：proactivity 限速与话题状态持久化到 `data/.proactivity_state.{session}.json`，重连不再清零爆发；主动话题由 LLM 去重（`core/proactivity.py`、`core/runtime_driver.py`、`core/inner_drive.py`、`prompts/system.py`）。

## 阶段 3：记忆层与解析健壮性

- M-07：删除 `emotion_summary` 双路径，prompt builder 只接冻结摘要（`prompts/system.py` + 各调用方）。
- #217：`upsert_fact` 冲突更新复活 `is_active`/`composite_score`，被 FactChecker 软删的事实可被用户重述救回（`storage/repository.py`）。
- #161：新增 `store_facts_bulk` 单事务批量 upsert，N+1 写入降为 1 次（`storage/repository.py`、`memory/long_term.py`、`memory/consolidation.py`）。
- #251：`search_reflections` 相关度检索（语义 0.6 + 关键词 0.4 混合评分），`extract_keywords` 公共化（`memory/retrieval.py`、`tools/memory_tools.py`、`models/memory.py`）。

  （#217/#161/#251 细节见 `changes/2026-07-17-记忆层三修复-issue217-161-251.md`）
- #213/#261：provider 流式响应 `resp.close()` + context manager、`/v1` 路径归一化、`ReadTimeout` 重试（`core/provider.py`）。
- #263：`async_utils` 无事件循环分支超时改 `wait_for`，异常不再被吞（`core/async_utils.py`）。
- #255：`_degrade_threshold`/`_max_fake_actions` 接入 config，不再硬编码 3（`core/agent.py`、`config.py`）。
- #144/#260：dispatcher Tier 3 裸 JSON 兜底补 `isinstance` 守卫，非 dict arguments 按空参数处理（`core/dispatcher.py`）。
- #262：CJK Extension A（U+3400~U+4DBF）纳入 token 估算；`doc/technical.md` 补 cl100k_base 与 DeepSeek tokenizer 的误差说明（`core/context_manager.py`、`doc/technical.md`）。
- M-15：CLI 流式输出加 XML 标签过滤状态机，不再打印 `<tool_call>`/`<think>` 原文（`core/cli_controller.py`）。

## 阶段 4：tools / prompts / config 清理（12 项）

见 `changes/2026-07-17-审计修复-阶段4-tools-prompts-config.md`（#273/#281/#282/#270/#271/#181 + L-02/L-03/L-05/L-07/L-09）。

## 阶段 5：Web 启动与死代码卫生

- L-08：shutdown 路径 personality 只写一次，去掉双写（`web/session.py`）。
- L-10/L-12：`load_config()` 收敛为单例调用，`web/server.py` 与 `web_main.py` 不再各调一次（`web/server.py`、`web_main.py`）。
- L-11：rate limiter 声明未用的 `_lock` 改为真 `threading.Lock` 并实际生效（`web/rate_limit.py`）。
- L-06：前端默认 AI 名改为 `'AI'`，不再硬编码"星"（`web/static/app.js`）。
- M-19：入口先 `setup_logging("INFO")` 再 `load_config()` 最后应用配置级别，启动期日志不再丢失（`main.py`、`web_main.py`）。
- H-04：llama-server 自动启动的 `--port` 从 `embedding_endpoint` 解析，自定义端口不再静默连错（`core/embedding_server.py`、`main.py`、`web_main.py`）。
- 死代码卫生：`update_experience_score`/`archive_observation` 加 session 过滤（M-01/M-02），`prune_old_turns` 改为真 per-session（M-03），ToolAgent 删重复过滤（`storage/repository.py`、`storage/database.py`、`core/tool_agent.py`）。

## 验证

- 测试基线：修复前 465 passed + 2 skipped → 修复后 **593 passed + 2 skipped**（全绿），新增约 130 个测试。
- 命令：`python -m pytest tests --ignore=tests/real_api -q`

## 未处理项及原因

- **审计滞后误标（13 个）**：#208/#268/#269/#272/#224/#238/#222/#220/#219/#191/#157/#299/#300——审计标"未修复"，逐行核实实际早已修复，仅审计取证滞后，无需改动。
- **不可达/已有保护（7 项）**：生产路径不可达或现有守卫已覆盖，验证后降级为 LOW，无需修复。
- **架构大改/需运行时验证（11 项）**：涉及架构层重构或只能在真实运行时确认，留待后续迭代。
- **M-14（部分处理）**：换 DeepSeek tokenizer 需引入外部依赖，违反项目零依赖原则；已将 CJK Ext A 纳入估算并在 `doc/technical.md` 写明误差，换 tokenizer 不做。
